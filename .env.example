# Database URL (For local testing with Postgres, uncomment and change)
# DATABASE_URL=postgresql://user:password@localhost/dbname

# API Base URL (Streamlit uses this to find the FastAPI backend)
# API_BASE_URL=http://127.0.0.1:8000
